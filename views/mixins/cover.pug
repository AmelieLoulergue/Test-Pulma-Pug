//- conditional display according to the second argument passed to the mixin
//- "lame" or "pb" => Different image size and title. Conditional display on 
//- the Call To Action because not the same organization
//- I think there are some simpler conditional display solutions in PUG 
//- that I don't know about ...
mixin cover(item, type)
    .box.fullheight
        .columns
            .column
                .media.is-hidden-mobile
                    .media-left
                    if(type === 'lame')
                        .image.is-200x300
                            img(src=item.mea)
                    else 
                        .image.is-96x96
                            img.mt-5(src=item.mea)
                            a.button.mt-6.is-primary(href=item.cta.href)= item.cta.txt

            .column.is-6.is-gapless
                p.text.tag.is-rounded.is-danger= item.tag
                if(type === 'lame')
                    p.is-size-3.mb-3= item.title
                else 
                    p.is-size-5=item.title
                p.title.is-4.has-text-primary= item.price
                p.text.is-size-7= item.mention

            if(type === 'lame')
                .column.is-flex.is-align-items-center.is-justify-content-flex-end
                    a.button.is-primary(href=item.cta.href)= item.cta.txt